'use client';
import {useEffect,useState} from 'react';
import {Sun,Moon,Monitor} from 'lucide-react';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
type Appearance='light'|'dark'|'system';
const options=[{value:'light',label:'浅色',icon:Sun},{value:'dark',label:'深色',icon:Moon},{value:'system',label:'跟随系统',icon:Monitor}];
export function ThemeSwitch(){const [appearance,setAppearance]=useState<Appearance>('system'),[ready,setReady]=useState(false);useEffect(()=>{try{const v=localStorage.getItem('resonance-appearance');if(v==='light'||v==='dark'||v==='system')setAppearance(v)}catch{}setReady(true)},[]);useEffect(()=>{if(!ready)return;const query=window.matchMedia('(prefers-color-scheme: dark)');const apply=()=>{const theme=appearance==='system'?(query.matches?'dark':'light'):appearance;document.documentElement.dataset.theme=theme;document.documentElement.style.colorScheme=theme};apply();try{localStorage.setItem('resonance-appearance',appearance)}catch{}query.addEventListener('change',apply);return()=>query.removeEventListener('change',apply)},[appearance,ready]);const Icon=options.find(o=>o.value===appearance)!.icon;return <section className="appearance-control" aria-label="外观设置"><span className="appearance-label">外观</span><Select value={appearance} items={options.map(({value,label})=>({value,label}))} onValueChange={v=>{if(v==='light'||v==='dark'||v==='system')setAppearance(v)}}><SelectTrigger aria-label="切换浅色或深色模式"><Icon size={16}/><SelectValue/></SelectTrigger><SelectContent side="top" align="start" alignItemWithTrigger={false}>{options.map(({value,label,icon:OptionIcon})=><SelectItem value={value} key={value}><span className="appearance-option"><OptionIcon size={16}/>{label}</span></SelectItem>)}</SelectContent></Select></section>}
